import React from 'react'
import { NavLink } from 'react-router-dom'

const NavNavigation = (link, name) => {
  return (
    <NavLink to={link}>
        n
    </NavLink>
  )
}

export default NavNavigation